import type { IMenuRepository } from '../../../domain/repositories/IMenuRepository';
export class GetMenuPublic { constructor(private repo: IMenuRepository) {} exec(id:number){ return this.repo.getMenuPublic(id);} }